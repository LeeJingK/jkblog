<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ljk.dao.CommentDao">

    <!--    <resultMap id="comment" type="Comment">-->
    <!--        <id property="id" column="id"/>-->
    <!--        <result property="nickname" column="nickname"/>-->
    <!--        <result property="email" column="email"/>-->
    <!--        <result property="content" column="content"/>-->
    <!--        <result property="adminComment" column="admin_comment"/>-->
    <!--        <result property="commentCount" column="comment_count"/>-->
    <!--        <result property="avatar" column="avatar"/>-->
    <!--        <result property="createTime" column="create_time"/>-->
    <!--        <result property="blogId" column="blog_id"/>-->
    <!--        <result property="parentCommentId" column="parent_comment_id"/>-->
    <!--        <association property="id" javaType="Blog">-->
    <!--            <id property="id" column="id"/>-->
    <!--        </association>-->
    <!--    </resultMap>-->

    <select id="findByBlogIdAndParentCommentNull" resultType="Comment">
        select c.id,c.nickname,c.email,c.content,c.avatar,
        c.create_time,c.blog_id,c.parent_comment_id
        from blogs.t_comment as c, blogs.t_blog as b
        where c.blog_id = b.id and c.blog_id = #{blogId} and c.parent_comment_id = #{blogParentId}
        order by c.create_time desc
    </select>

    <insert id="saveComment" parameterType="Comment">
        insert into blogs.t_comment (nickname,email,content,avatar,
        create_time,blog_id,parent_comment_id, admin_comment)
        values (#{nickname},#{email},#{content},#{avatar},
        #{createTime},#{blogId},#{parentCommentId}, #{adminComment})
    </insert>


    <select id="findByParentCommentId" resultType="Comment">
        select c.id, c.nickname, c.email, c.content, c.avatar,
        c.create_time, c.blog_id, c.parent_comment_id
        from blogs.t_comment as c
        where c.parent_comment_id = #{parentCommentId}
    </select>

    <select id="findByBlogIdParentIdNull" resultType="Comment">
        select *
        from blogs.t_comment as c
        where c.blog_id = #{blogId} and c.parent_comment_id = #{blogParentId}
        order by c.create_time desc
    </select>

    <!--查询一级子评论-->
    <select id="findByBlogIdParentIdNotNull" resultType="Comment">
        select *
        from blogs.t_comment as c
        where c.blog_id = #{blogId} and c.parent_comment_id = #{id}
        order by c.create_time desc
    </select>

    <!--查询二级子评论-->
    <select id="findByBlogIdAndReplayId" resultType="Comment">
        select *
        from blogs.t_comment as c
        where c.blog_id = #{blogId} and c.parent_comment_id = #{childId}
        order by c.create_time desc
    </select>
    <!--删除评论-->
    <delete id="deleteComment">
        delete from  blogs.t_comment where id = #{id}
    </delete>

</mapper>